import fetch from 'node-fetch';
//
/** 
*  Autor : Josepina Martin
*  Software Engineering Intern
* Deep Catch Namibia Holdings
* Tecnical Assessment 
/*

const ENDPOINT = 'https://nt-cdn.s3.amazonaws.com/colors.json';


/**
 * @param  colour name
 * @param  hex filter for code
 * @param  comName filter for complementary colour name
 * @param  CompHex filter for complememntary color Hex code
 * @returns Promise
*/

const fetchColors = async ({name,hex,compname,CompHex}) =>{
  const res = fetch(ENDPOINTS);
 /*
   var filterColors = color.filter (almond,pertwinkle,white,shadow,black (names)
     return res[filter]=== fetchColors;
  */console.log(response);

return res;
};

//
export default fetchColors;
